# CLIVE v0.10 — Agent Invocation Prompts

**Version:** v0.10 — Users + Trust Zones  
**Blocks:** 6 (Users), 7 (Trust Zones) — primary; 8, 9, 16, 17, 23 — touched  
**Produced by:** Architect, 2026-05-17  
**Highest decision ID at time of writing:** D-144

---

## How to use this document

Each section below is a complete prompt for a separate Claude Code session.
Open one session per wave. Waves within the same numbered group can be
opened simultaneously. Do not start Wave 2 until Wave 1 is committed.

```
Wave 0   [Architect]          Record D-142 (v0.9 sign-off) + confirm D-143/D-144 present
            │
Wave 1   [Knowledge]          Schema: users, zones, tool_registry migration,
                               pending_actions zone_scope — four SQL init files
            │
     ┌──────┴──────┐
Wave 2   [Intel]    [Access]  ← open simultaneously
         Block 8/9: Block 23:
         zone         /whoami +
         validation + DB-backed
         zone_scope   auth
         propagation
            │
Wave 3   [Infra]              CI zone boundary tests + CI verification
```

**Critical path:** Wave 1 (schema) must be committed before Wave 2 begins.
Both Wave 2 sessions must be committed before Wave 3.

**Prerequisite before Wave 0:** D-142 (CLIVE v0.9 signed off) must be present in
DECISIONS.md. v0.9 sign-off requires the Wave 4 Grafana "Worker Runs (24h)" panel
to be added and CI to pass. If D-142 is absent, complete v0.9 Wave 4 first.

**What is already in place (do not re-implement):**
- `zone_of_origin TEXT NOT NULL DEFAULT 'personal'` on `clive_search.chunks`
- `zone_scope TEXT NOT NULL DEFAULT 'personal'` on `clive_state.system_documents`
- `WHERE zone_of_origin = $2` in `retrieval.py` retrieve() — zone filtering already
  explicit in SQL; v0.10 adds zone existence validation on top of this
- `alignment.py` already checks zone_boundary_violation on cross-zone data
- `auth.py` `is_authenticated()` compares chat_id to `TELEGRAM_OWNER_CHAT_ID` env var
- No `users` table, no `zones` table, no `zone_permissions` on tool_registry,
  no `zone_scope` on pending_actions

---

## Wave 0 — Architect (run first, in this session)

> **This is not a copy-paste prompt.** This is work for the current
> Architect session.
>
> **Check:** Confirm D-142 (v0.9 signed off) is present in DECISIONS.md.
> If absent, stop — do not begin v0.10 until v0.9 is formally closed.
>
> Confirm D-143 and D-144 are present. They were recorded when this
> prompts document was produced. If present, proceed.
>
> No implementation in this wave. Wave 0 is complete when D-142 exists.

---

## Wave 1 — Knowledge Agent (schema)

Open a new Claude Code session. Paste the prompt below.

```
Read DECISIONS.md from the repo root and confirm the highest decision ID
before acting on anything else. If D-143 and D-144 are not present, stop
and tell me — do not proceed.

CONTEXT:
CLIVE v0.9 is signed off. v0.10 has been scoped (D-143) and acceptance
criteria recorded (D-144). Your task is the Block 6/7/16 storage layer
for users and zones. No application logic. No Block 8, 9, 13, or 23 changes.

WHAT ALREADY EXISTS — READ THESE BEFORE WRITING ANYTHING:
- infrastructure/sql/init/ — list all files to confirm next sequence number
  (should be 17; files 01–16 exist; DO NOT reuse any number)
- infrastructure/sql/init/12_v08_tool_registry.sql — exact tool_registry schema
- infrastructure/sql/init/07_v03_tables.sql — exact pending_actions schema
- infrastructure/sql/init/13_v08_tool_registry_seed.sql — seed pattern to follow

CURRENT STATE:
- clive_search.chunks already has zone_of_origin TEXT DEFAULT 'personal'
- clive_state.system_documents already has zone_scope TEXT DEFAULT 'personal'
- clive_state.tool_registry: no zone_permissions column yet
- clive_state.pending_actions: no zone_scope column yet
- No users table. No zones table.

YOUR TASK — Four SQL files in infrastructure/sql/init/:

1. NEW FILE: infrastructure/sql/init/17_v10_users_zones.sql

   Table clive_state.zones:
     zone_name    TEXT        PRIMARY KEY,
     description  TEXT        NOT NULL,
     created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW()

   Table clive_state.users:
     user_id          UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
     telegram_chat_id BIGINT      UNIQUE NOT NULL,
     role             TEXT        NOT NULL
                                  CHECK (role IN ('owner', 'viewer')),
     zone_access      TEXT[]      NOT NULL DEFAULT ARRAY['personal'],
     created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW()

   Indexes:
     idx_users_telegram_chat_id  ON clive_state.users (telegram_chat_id)
     idx_users_role              ON clive_state.users (role)

   Grants:
     GRANT ALL ON clive_state.zones TO clive_app;
     GRANT ALL ON clive_state.users TO clive_app;

   Must be fully idempotent: CREATE TABLE IF NOT EXISTS, CREATE INDEX IF NOT EXISTS.
   No owner row seeded here — owner is registered by Block 23 at startup (D-144).

2. NEW FILE: infrastructure/sql/init/18_v10_zones_seed.sql

   INSERT into clive_state.zones (ON CONFLICT DO NOTHING):
     zone_name = 'personal'
     description = 'Default personal zone — all owner data'

   This is the only seed. No user row here.
   Must be idempotent: ON CONFLICT DO NOTHING.

3. NEW FILE: infrastructure/sql/init/19_v10_tool_registry_zone_permissions.sql

   Add column to clive_state.tool_registry:
     ALTER TABLE clive_state.tool_registry
       ADD COLUMN IF NOT EXISTS zone_permissions TEXT[] NOT NULL DEFAULT ARRAY['personal'];

   Update all existing tool rows to set zone_permissions = ARRAY['personal']
   (safe idempotent UPDATE — setting to same value on re-run is harmless):
     UPDATE clive_state.tool_registry
       SET zone_permissions = ARRAY['personal']
       WHERE zone_permissions IS NULL OR zone_permissions = '{}';

   No new indexes needed. No grant changes (tool_registry already grants to clive_app).
   Must be idempotent: ADD COLUMN IF NOT EXISTS.

4. NEW FILE: infrastructure/sql/init/20_v10_pending_actions_zone_scope.sql

   Add column to clive_state.pending_actions:
     ALTER TABLE clive_state.pending_actions
       ADD COLUMN IF NOT EXISTS zone_scope TEXT NOT NULL DEFAULT 'personal';

   Index for zone-scoped action lookups (future use):
     CREATE INDEX IF NOT EXISTS idx_pending_actions_zone
       ON clive_state.pending_actions (zone_scope)
       WHERE status = 'pending';

   No grant changes (pending_actions already grants to clive_app).
   Must be idempotent: ADD COLUMN IF NOT EXISTS, CREATE INDEX IF NOT EXISTS.

CONSTRAINTS:
- Schema only. Do not touch Block 8, 9, 13, or 23 source code.
- clive_audit_writer has INSERT-only on audit table — do not alter that privilege.
- New tables use clive_app only. No new roles.
- No secrets in SQL files.
- File numbers: 17, 18, 19, 20. Verify against existing init/ listing before writing.

DONE WHEN:
- 17_v10_users_zones.sql: idempotent, creates users + zones tables with indexes
- 18_v10_zones_seed.sql: single 'personal' zone row with ON CONFLICT DO NOTHING
- 19_v10_tool_registry_zone_permissions.sql: idempotent ADD COLUMN + UPDATE
- 20_v10_pending_actions_zone_scope.sql: idempotent ADD COLUMN + index
- No filename conflicts with existing init files
- Changes committed to main
```

---

## Wave 2-A — Intelligence Agent (Block 8 zone validation + Block 9 zone propagation)

Wait for Wave 1 to be committed. Confirm 17_v10_users_zones.sql exists before starting.

```
Read DECISIONS.md from the repo root and confirm the highest decision ID
before acting on anything else. If D-143 and D-144 are not present, stop.
Confirm infrastructure/sql/init/17_v10_users_zones.sql exists.
If absent, stop — Wave 1 must be committed first.

CONTEXT:
v0.10 adds clive_state.users and clive_state.zones tables (Wave 1). Your task
is two changes to existing source code:

1. BLOCK 8 ZONE VALIDATION: Before retrieval SQL runs, validate that the
   requested zone_scope is a known zone in clive_state.zones. Unknown zones
   return empty results immediately (no SQL against chunks).

2. BLOCK 9 ZONE PROPAGATION: When action.py inserts a pending_actions row,
   carry the zone_scope from the requesting event into the new zone_scope column.

READ FIRST (all of these):
- src/orchestrator/orchestrator/retrieval.py (full — your primary file)
- src/orchestrator/orchestrator/action.py (full — pending_actions insert)
- infrastructure/sql/init/17_v10_users_zones.sql (zones table schema)
- infrastructure/sql/init/20_v10_pending_actions_zone_scope.sql (new column)
- src/orchestrator/orchestrator/main.py (event payload patterns for zone_scope)

CHANGE 1 — BLOCK 8 ZONE VALIDATION in retrieval.py:

Add a new function:
  async def _is_valid_zone(zone_scope: str) -> bool:
      """Check that zone_scope exists in clive_state.zones.

      D-143: zone validation before retrieval. Returns False for unknown zones.
      Non-fatal DB error: log WARN and return True (fail open — do not block
      retrieval on DB unavailability; zone boundary enforcement is best-effort
      at this layer when DB is degraded).
      """
      if _pool is None:
          return True   # pool not init'd — fail open
      try:
          async with _pool.acquire() as conn:
              row = await conn.fetchrow(
                  "SELECT 1 FROM clive_state.zones WHERE zone_name = $1",
                  zone_scope,
              )
          return row is not None
      except Exception as exc:  # noqa: BLE001
          log.warning("zone_validation_error", zone_scope=zone_scope, exc=str(exc))
          return True   # fail open on DB error

Modify retrieve() function:
  At the start of retrieve(), before the conn.fetch(...) call, add:
    if not await _is_valid_zone(zone_scope):
        log.warning(
            "retrieval_unknown_zone",
            zone_scope=zone_scope,
            conversation_id=str(conversation_id),
        )
        return {"ranked_chunks": [], "result_count": 0}

  Also add the same check at the start of retrieve_document_by_filename()
  and retrieve_document_list():
    if not await _is_valid_zone(zone_scope):
        log.warning("retrieval_unknown_zone", zone_scope=zone_scope)
        return <appropriate empty result for the function>

  For retrieve_document_by_filename: return {"source_keys": [], "chunk_count": 0}
  For retrieve_document_list: return {"documents": [], "total": 0}

  Do NOT add validation to retrieve_system_document() — system documents use
  zone_scope but are not per-user data; the block already validates by document_type.

CHANGE 2 — BLOCK 9 ZONE PROPAGATION in action.py:

READ action.py fully before changing anything.

In the function that inserts into clive_state.pending_actions:
  a. Add zone_scope as a parameter (with default 'personal')
  b. Include zone_scope in the INSERT statement:
       INSERT INTO clive_state.pending_actions
         (action_request_id, action_type, action_target, action_description,
          conversation_id, chat_id, expires_at, zone_scope)
       VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
     (adjust the parameter number to match your read of action.py)

In the calling code (wherever action.py is called from main.py):
  Pass zone_scope from event.payload.get('zone_scope', 'personal').
  Follow the existing event.payload access patterns in main.py.

CONSTRAINTS:
- D-003: no direct block-to-block calls. retrieval.py is a sub-module of Block 13
  (orchestrator-mediated, D-043). The zone validation DB call is permitted here
  (same pattern as existing retrieval calls).
- D-050: 'personal' is always a valid zone. The zones table is seeded with it
  (Wave 1). Zone validation will always pass for 'personal'.
- Fail-open on DB error: zone validation must not make retrieval more fragile.
  Log WARN; return True when DB is unavailable.
- No new dependencies. No new pools — use the existing _pool in retrieval.py.
- action.py: the zone_scope column was added by Wave 1 (SQL file 20). If zone_scope
  column is missing at test time (test DB not yet updated), the INSERT will fail.
  Ensure test DB SQL init files include file 20 before running tests.

DONE WHEN:
- retrieval.py has _is_valid_zone() function
- retrieve(), retrieve_document_by_filename(), retrieve_document_list() return
  empty results for unknown zones
- action.py propagates zone_scope from event into pending_actions INSERT
- Existing tests pass
- New unit tests (src/orchestrator/tests/test_v10_zone_validation.py):
  - _is_valid_zone returns True for 'personal' (valid zone in test DB)
  - _is_valid_zone returns False for 'nonexistent_zone'
  - retrieve() returns empty result when zone_scope='nonexistent_zone'
  - retrieve() proceeds normally when zone_scope='personal'
  - _is_valid_zone returns True when pool is None (fail open)
  - action pending_actions INSERT includes zone_scope field
- Changes committed to main
```

---

## Wave 2-B — Access & Security Agent (Block 23 /whoami + DB-backed auth)

Open simultaneously with Wave 2-A. Wait for Wave 1. Confirm 17_v10_users_zones.sql exists.

```
Read DECISIONS.md from the repo root and confirm the highest decision ID
before acting on anything else. If D-143 and D-144 are not present, stop.
Confirm infrastructure/sql/init/17_v10_users_zones.sql exists.
If absent, stop — Wave 1 must be committed first.

CONTEXT:
v0.10 adds clive_state.users (Block 6) and clive_state.zones (Block 7) tables.
Your task is two changes to the Telegram service (Block 23):

1. /whoami COMMAND: New Telegram command that returns the caller's user profile
   from clive_state.users.

2. DB-BACKED AUTHENTICATION: is_authenticated() in auth.py moves from pure
   env-var to an in-memory cache populated from clive_state.users, with env-var
   as bootstrap fallback. Owner record is upserted from env var on startup.

READ FIRST (all of these):
- src/telegram/clive_telegram/auth.py (full — primary file for auth changes)
- src/telegram/clive_telegram/bot.py (full — command handler patterns)
- src/telegram/clive_telegram/main.py (startup and DB pool patterns)
- src/telegram/pyproject.toml (dependencies)
- infrastructure/sql/init/17_v10_users_zones.sql (users table schema)
- infrastructure/sql/init/18_v10_zones_seed.sql (zones seed)

CHANGE 1 — DB-BACKED AUTHENTICATION in auth.py:

The Telegram service needs a DB connection pool to clive_state.users. Follow
the pattern in src/orchestrator/orchestrator/retrieval.py for pool setup.

a. Add module-level pool:
   _pool: asyncpg.Pool | None = None
   _allowed_chat_ids: set[int] = set()
   _AUTH_CACHE_REFRESH_SECONDS = 60

b. Add pool init:
   async def init_pool() -> None:
       """Initialise auth DB pool (clive_app role)."""
       global _pool
       dsn = (
           f"postgresql://clive_app:{os.environ['APP_DB_PASSWORD']}"
           f"@{os.environ.get('POSTGRES_HOST', 'postgres')}:5432/clive"
       )
       _pool = await asyncpg.create_pool(dsn, min_size=1, max_size=3)
       log.info("auth_pool_initialised")

c. Add owner startup upsert:
   async def register_owner_if_absent() -> None:
       """Upsert owner record from TELEGRAM_OWNER_CHAT_ID env var.

       D-144: owner is registered by Block 23 at startup.
       Uses INSERT ... ON CONFLICT DO NOTHING to be idempotent.
       If pool not ready: log WARNING and skip (non-fatal).
       """
       if _pool is None:
           log.warning("auth_pool_not_ready_for_owner_registration")
           return
       owner_chat_id = get_owner_chat_id()
       async with _pool.acquire() as conn:
           await conn.execute(
               """
               INSERT INTO clive_state.users
                 (telegram_chat_id, role, zone_access)
               VALUES ($1, 'owner', ARRAY['personal'])
               ON CONFLICT (telegram_chat_id) DO NOTHING
               """,
               owner_chat_id,
           )
       log.info("owner_registration_checked", chat_id=owner_chat_id)

d. Add cache refresh:
   async def refresh_auth_cache() -> None:
       """Load allowed telegram_chat_ids from users table into _allowed_chat_ids.

       Runs at startup and every _AUTH_CACHE_REFRESH_SECONDS seconds.
       Falls back to env var if pool is not ready.
       """
       global _allowed_chat_ids
       if _pool is None:
           _allowed_chat_ids = {get_owner_chat_id()}
           return
       try:
           async with _pool.acquire() as conn:
               rows = await conn.fetch(
                   "SELECT telegram_chat_id FROM clive_state.users"
               )
           _allowed_chat_ids = {row["telegram_chat_id"] for row in rows}
           if not _allowed_chat_ids:
               # Bootstrap: no users yet — fall back to env var
               _allowed_chat_ids = {get_owner_chat_id()}
               log.warning("auth_cache_empty_using_env_var_fallback")
       except Exception as exc:  # noqa: BLE001
           log.warning("auth_cache_refresh_error", exc=str(exc))
           if not _allowed_chat_ids:
               _allowed_chat_ids = {get_owner_chat_id()}

e. Add background refresh loop:
   async def auth_cache_refresh_loop() -> None:
       """Background coroutine: refresh auth cache every 60 seconds."""
       while True:
           try:
               await asyncio.sleep(_AUTH_CACHE_REFRESH_SECONDS)
               await refresh_auth_cache()
           except asyncio.CancelledError:
               return

f. Update is_authenticated() — keep it synchronous (bot.py calls it everywhere):
   def is_authenticated(chat_id: int) -> bool:
       """Return True if chat_id is in the allowed set.

       Cache is populated from clive_state.users at startup and refreshed
       every 60 seconds. Falls back to env-var check if cache is empty.
       D-057: channel membership is the authentication factor.
       """
       if _allowed_chat_ids:
           authenticated = chat_id in _allowed_chat_ids
       else:
           authenticated = chat_id == get_owner_chat_id()
       if not authenticated:
           log.warning("unauthenticated_message", chat_id=chat_id)
       return authenticated

g. Add user lookup helper (for /whoami):
   async def get_user_profile(chat_id: int) -> dict | None:
       """Return user record from clive_state.users for this chat_id, or None."""
       if _pool is None:
           return None
       async with _pool.acquire() as conn:
           row = await conn.fetchrow(
               """
               SELECT user_id, telegram_chat_id, role, zone_access, created_at
               FROM clive_state.users
               WHERE telegram_chat_id = $1
               """,
               chat_id,
           )
       if not row:
           return None
       return {
           "user_id": str(row["user_id"]),
           "telegram_chat_id": row["telegram_chat_id"],
           "role": row["role"],
           "zone_access": list(row["zone_access"]),
           "created_at": row["created_at"].isoformat(),
       }

MODIFY src/telegram/clive_telegram/main.py:
  a. Import auth module: from .auth import init_pool, register_owner_if_absent,
     refresh_auth_cache, auth_cache_refresh_loop
  b. In startup: await auth.init_pool()
  c. After pool init: await auth.register_owner_if_absent()
  d. After register: await auth.refresh_auth_cache()
  e. Start background task: asyncio.create_task(auth.auth_cache_refresh_loop())
     (alongside other background tasks if any exist)
  f. Update startup log to note v0.10 DB-backed auth.

Add asyncio import to auth.py if not already present.
Add asyncpg to src/telegram/pyproject.toml if not already present.

CHANGE 2 — /whoami COMMAND in bot.py:

Add handler function following the pattern of other command handlers:
  async def whoami_command(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
      """Return the caller's user profile from clive_state.users.

      D-144: /whoami returns telegram_chat_id, role, zone_access.
      """
      chat_id = update.effective_chat.id
      if not is_authenticated(chat_id):
          return
      
      profile = await auth.get_user_profile(chat_id)
      
      if profile is None:
          text = (
              "You are authenticated but not yet registered in the users table.\n"
              "This resolves on next service restart."
          )
      else:
          zones = ", ".join(profile["zone_access"]) or "none"
          text = (
              f"*User Profile*\n"
              f"Chat ID: `{profile['telegram_chat_id']}`\n"
              f"Role: `{profile['role']}`\n"
              f"Zone access: `{zones}`\n"
              f"Member since: {profile['created_at'][:10]}"
          )
      
      await update.message.reply_text(text, parse_mode="Markdown")

Register the handler in the application setup section:
  application.add_handler(CommandHandler("whoami", whoami_command))

Update the /help command text (wherever COMMAND_HELP or similar is defined)
to include: /whoami — show your user profile and zone access

CONSTRAINTS:
- D-003: auth.py queries DB directly (it is co-located in Block 23, not a
  separate block). This is the same pattern as reminder_handler.py in Block 13.
- D-057: channel-as-authentication is preserved. We are adding DB backing to
  the same channel-based check, not replacing the model.
- D-001: single-owner system. The /whoami command is owner-only (gated by
  is_authenticated). No multi-user UI is exposed.
- Fail-open auth: if the auth cache is empty and the DB is unreachable, fall
  back to env var. Auth must not make the bot unresponsive.
- No new external dependencies beyond asyncpg (already used in orchestrator;
  add to telegram's pyproject.toml only if not present).
- Secrets: APP_DB_PASSWORD from environment. Do not hardcode. Pattern:
  load_dotenv('/etc/clive/secrets.env') already in main.py.

DONE WHEN:
- auth.py has init_pool(), register_owner_if_absent(), refresh_auth_cache(),
  auth_cache_refresh_loop(), updated is_authenticated(), get_user_profile()
- main.py starts auth pool, registers owner, starts refresh loop
- bot.py has /whoami command handler
- /help text updated to include /whoami
- asyncpg in src/telegram/pyproject.toml (verify; add if absent)
- Existing tests pass
- New tests (src/telegram/tests/test_v10_auth.py):
  - is_authenticated returns True when chat_id in _allowed_chat_ids
  - is_authenticated returns False when chat_id not in _allowed_chat_ids
  - is_authenticated falls back to env var when _allowed_chat_ids is empty
  - refresh_auth_cache populates _allowed_chat_ids from mock DB rows
  - get_user_profile returns dict with correct fields for known chat_id
  - get_user_profile returns None for unknown chat_id
- New test (src/telegram/tests/test_v10_whoami.py):
  - /whoami handler returns profile text when user found
  - /whoami handler returns "not yet registered" when profile is None
- Changes committed to main
```

---

## Wave 3 — Infrastructure Agent (CI zone boundary tests + CI verification)

Wait for Wave 2-A AND Wave 2-B to both be committed. Then open a new session.

```
Read DECISIONS.md from the repo root and confirm the highest decision ID
before acting on anything else. If D-143 and D-144 are not present, stop.
Confirm ALL of these exist before proceeding:
  - infrastructure/sql/init/17_v10_users_zones.sql
  - infrastructure/sql/init/18_v10_zones_seed.sql
  - infrastructure/sql/init/19_v10_tool_registry_zone_permissions.sql
  - infrastructure/sql/init/20_v10_pending_actions_zone_scope.sql
  - src/orchestrator/tests/test_v10_zone_validation.py
  - src/telegram/tests/test_v10_auth.py
If any is missing, stop — earlier waves must complete first.

CONTEXT:
v0.10 implementation is complete (Waves 1–2). Your task: add zone boundary
integration tests to the CI database privilege test suite, verify the full
CI pipeline will pass, and confirm SQL idempotency.

PART 1 — ZONE BOUNDARY CI INTEGRATION TESTS

Read .github/workflows/ci.yml. There is a test-db job that runs integration
tests against a real PostgreSQL instance. You will add zone boundary assertions
to this job.

ADD the following steps to the test-db job in ci.yml, after the existing
privilege assertions:

Step name: "Assert zone 'personal' exists in zones table (v0.10, D-143)"
  psql -h localhost -U postgres -d clive -c \
    "SET ROLE clive_app; \
     SELECT 1 FROM clive_state.zones WHERE zone_name = 'personal';" \
    | grep -q "1 row"

Step name: "Assert owner user row exists in users table (v0.10, D-144)"
  Note: The owner row is inserted by Block 23 startup, not by SQL init.
  Assert the TABLE exists and schema is correct instead:
  psql -h localhost -U postgres -d clive -c \
    "SET ROLE clive_app; \
     SELECT telegram_chat_id, role, zone_access FROM clive_state.users LIMIT 1;"
  (This passes even if empty — just confirms table and columns exist with correct types.)

Step name: "Assert tool_registry rows have zone_permissions (v0.10, D-143)"
  psql -h localhost -U postgres -d clive -c \
    "SET ROLE clive_app; \
     SELECT count(*) FROM clive_state.tool_registry \
     WHERE zone_permissions IS NULL OR zone_permissions = '{}';" \
    | grep -q "0"
  (Zero rows should have NULL or empty zone_permissions after seed + migration.)

Step name: "Assert pending_actions has zone_scope column (v0.10, D-144)"
  psql -h localhost -U postgres -d clive -c \
    "SET ROLE clive_app; \
     SELECT zone_scope FROM clive_state.pending_actions LIMIT 1;"
  (Passes if column exists, even if table is empty.)

Step name: "Assert clive_app cannot CREATE tables (least-privilege check)"
  psql -h localhost -U postgres -d clive -c \
    "SET ROLE clive_app; \
     CREATE TABLE clive_state.test_should_fail (id int);" \
    2>&1 | grep -q "permission denied"

PART 2 — SQL IDEMPOTENCY CHECK

Read all four new SQL files:
- infrastructure/sql/init/17_v10_users_zones.sql
- infrastructure/sql/init/18_v10_zones_seed.sql
- infrastructure/sql/init/19_v10_tool_registry_zone_permissions.sql
- infrastructure/sql/init/20_v10_pending_actions_zone_scope.sql

Confirm for each:
- Every CREATE uses CREATE TABLE IF NOT EXISTS / CREATE INDEX IF NOT EXISTS
- Every INSERT uses ON CONFLICT DO NOTHING
- Every ALTER uses ADD COLUMN IF NOT EXISTS
- No bare DROP or TRUNCATE statements

If any statement is not idempotent: flag it as a comment in this session output
with the exact file and line number. Do not fix — report and stop on that file.

The existing CI idempotency test already runs all files twice (glob pattern).
These four files will be picked up automatically. Confirm the glob pattern in
ci.yml covers infrastructure/sql/init/ without an explicit file list.

PART 3 — TEST COVERAGE VERIFICATION

Read each test file listed below. Confirm the assertions are present. If any
test file is missing or a specific assertion is absent: add it to
docs/v0.10-ci-gap-report.md (create the file if needed).

src/orchestrator/tests/test_v10_zone_validation.py (Wave 2-A):
  - _is_valid_zone returns True for 'personal'
  - _is_valid_zone returns False for 'nonexistent_zone'
  - retrieve() returns empty result for unknown zone
  - retrieve() proceeds normally for 'personal' zone
  - _is_valid_zone returns True when pool is None (fail open)

src/telegram/tests/test_v10_auth.py (Wave 2-B):
  - is_authenticated returns True when chat_id in _allowed_chat_ids
  - is_authenticated returns False when not in set
  - is_authenticated falls back to env var when set is empty
  - refresh_auth_cache populates cache from DB rows

src/telegram/tests/test_v10_whoami.py (Wave 2-B):
  - /whoami returns profile text when user found
  - /whoami returns "not yet registered" when profile is None

PART 4 — DOCKER COMPOSE INIT ORDER

Confirm that SQL init files 17–20 run in correct order (after 16).
The existing issue: file 10 appears twice (10_v07_actions_tables.sql and
10_v07_memory_tables.sql). This is pre-existing and already handled by glob.
Files 17–20 should sort correctly after 16 with no conflicts.
Confirm only — no change needed if order is correct.

DONE WHEN:
- ci.yml has four new zone boundary integration test steps in test-db job
- SQL idempotency confirmed for all four new files (or gap report with exact
  file + line reference)
- All test assertions confirmed present, or gap report at docs/v0.10-ci-gap-report.md
- Changes committed to main
```

---

## Sign-off (Owner — after Wave 3 completes)

Once the Infrastructure Agent session reports clean (or any gap report is addressed),
v0.10 is ready for sign-off. The owner records:

- **D-145** — CLIVE v0.10 signed off (with date and confirmation all ten
  acceptance criteria from D-144 are met)

This follows the established pattern: D-094 (v0.1), D-104 (v0.2), D-110 (v0.3),
D-120 (v0.4), D-124 (v0.5), D-127 (v0.6), D-130/D-133 (v0.7), D-139 (v0.8),
D-142 (v0.9).

After v0.10 signs off, v0.11 (Multi-Surface + Sync/State) is unblocked per D-136.

---

## v0.10 context for the next Architect session

**What will exist after v0.10:**

| Artefact | State |
|---|---|
| clive_state.users | Owner registered by Block 23 startup; viewer role schema-capable |
| clive_state.zones | 'personal' zone seeded; zone_name is the enforcement key |
| tool_registry.zone_permissions | All existing tools: ARRAY['personal'] |
| pending_actions.zone_scope | Propagated from event payload; defaults 'personal' |
| retrieval.py | Zone existence validated before any retrieval SQL |
| auth.py | In-memory cache from users table; env var bootstrap fallback |
| Block 23 commands | /whoami added; all others unchanged |

**Partial blocks after v0.10:**
- Block 3: Tool management UX designed (v0.8); full close at v0.11 (both surfaces)
- Block 4: Still collapsed into Block 23; becomes real at v0.11
- Block 19: Tool management + system doc activation; full close at v0.12

**Not started, planned:**
- Block 2 (Multi-Surface): v0.11 — now unblocked by Blocks 6 + 7
- Block 5 (Sync/State): v0.11
- Block 21 (Evolution Engine): Gated (post-v1)
- Block 24 (Sandboxing): Stub at v1.0
- Block 26 (Physical Device): Gated (D-135)

---

*v0.10 prompt document — produced by Architect 2026-05-17*  
*Do not edit without a corresponding decision record.*
