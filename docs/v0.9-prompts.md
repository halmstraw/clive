# CLIVE v0.9 — Agent Invocation Prompts

**Version:** v0.9 — Workers + Background Agents  
**Blocks:** 10 (primary), 12 (closes partial)  
**Produced by:** Architect, 2026-05-16  
**Highest decision ID at time of writing:** D-138

---

## How to use this document

Each section below is a complete prompt for a separate Claude Code session.
Open one session per wave. Waves within the same numbered group can be
opened simultaneously. Do not start Wave 2 until Wave 1 sessions have
committed their output.

```
Wave 0   [Architect]          Record D-140 + D-141 (D-139 must exist first)
            │
     ┌──────┴──────┐
Wave 1   [Knowledge]    [Intel-A]       ← open simultaneously
         Schema:        Block 12
         workers,       policy
         worker_runs,   document
         chunk tracking
            │
     ┌──────┴──────────────────┐
Wave 2   [Systems]    [Intel-B]         ← open simultaneously
         Block 13     Block 8:
         scheduler    retrieval
         + scope      tracking +
         enforcement  Block 12
                      enforcement
            │
     ┌──────┴──────┐
Wave 3   [Intel-C]   [Intel-D]         ← open simultaneously
         Daily        Knowledge
         Digest       Maintenance
         Worker       Worker
            │
Wave 4   [Infra]                        ← sequential, last
         Grafana panel +
         CI verification
```

**Critical path:** Wave 1-A (Knowledge Agent schema) must be committed before
Wave 2 sessions begin. Wave 1-B (Block 12 policy doc) must be committed before
Wave 2-B begins. Wave 2 must fully complete before Wave 3. Wave 4 is last.

**Prerequisite before Wave 0:** D-139 (CLIVE v0.8 signed off) must be present
in DECISIONS.md. If it is absent, the owner must record v0.8 sign-off first.

---

## Wave 0 — Architect (run first, in this session)

> **This is not a copy-paste prompt.** This is work for the current
> Architect session before any other session is opened.
>
> **Check first:** Confirm D-139 (v0.8 signed off) is present in DECISIONS.md.
> If it is absent, stop — do not record v0.9 decisions until v0.8 is formally
> closed.
>
> Record two decisions in DECISIONS.md:
>
> **D-140** — v0.9 scope: Block 10 (Workers / Background Agents) primary;
> Block 12 (Context Window) closes partial. Two workers: daily_digest (proactive
> daily Telegram summary) and knowledge_maintenance (weekly stale chunk flagging
> via confirmation gate). No new Telegram commands. No new surfaces.
>
> **D-141** — v0.9 acceptance criteria:
> 1. `workers` table in PostgreSQL with FK to tool_registry, schedule_type,
>    cron_expression/trigger_event, execution_scope, last_run_at; `worker_runs`
>    table logs execution (run_id, worker_name, triggered_at, completed_at,
>    status, outcome_summary, error_detail); `daily_digest` and
>    `knowledge_maintenance` registered in both tool_registry and workers tables
> 2. Block 13 scheduler starts at orchestrator startup, loads worker schedules
>    from DB, triggers `daily_digest` at 08:00 UTC daily (cron: `0 8 * * *`)
>    and `knowledge_maintenance` at 09:00 UTC every Monday (cron: `0 9 * * 1`)
> 3. Scope enforcement active: workers receive a scoped capability dict limited
>    to their declared execution_scope; Block 13 logs a WARN and skips any
>    capability call outside that scope
> 4. Worker outcomes logged to `clive_state.worker_runs`; Prometheus counter
>    `clive_worker_runs_total{worker_name, status}` emitted per run; Grafana
>    panel added to existing dashboard showing run history by worker and status
> 5. Daily digest worker runs on cron schedule, queries last-24h data (query
>    count, confirmed actions, LLM spend, feedback count, system health),
>    formats summary, delivers via Telegram /alert endpoint
> 6. Knowledge maintenance worker runs weekly, queries chunks with
>    retrieval_count = 0 AND created_at older than threshold (default 90 days),
>    presents up to 5 per run as a single batch to owner via Block 9
>    confirmation gate (action_type = 'knowledge.prune'); no autonomous
>    deletion — D-006 applies; handle_prune_confirmed deletes only after
>    owner confirms
> 7. Block 8 increments retrieval_count and sets last_retrieved_at on all
>    returned chunks after each successful query (non-fatal; try/except)
> 8. Block 12 closes: policy document at
>    `docs/spec/Block 12 - Context Window Policy.md`; context.py declares
>    all policy parameters as named constants and references the policy document
>    in its module docstring
> 9. CI passes

---

## Wave 1-A — Knowledge Agent (schema)

Open a new Claude Code session. Paste the prompt below.

```
Read DECISIONS.md from the repo root and confirm the highest decision ID
before acting on anything else. If D-140 and D-141 are not present, stop
and tell me — do not proceed.

CONTEXT:
CLIVE v0.8 is signed off. v0.9 has been scoped (D-140) and acceptance
criteria recorded (D-141). Your task is the Block 10 / Block 16 storage
layer for workers. No application logic. No Block 13 or Block 8 changes.

CURRENT SYSTEM STATE:
- Block 17 (Tool Registry): clive_state.tool_registry table exists
  (see infrastructure/sql/init/12_v08_tool_registry.sql for exact schema)
- clive_search.chunks: exists with chunk_id UUID, document_id UUID, content,
  embedding, source_attribution, zone_of_origin, position, metadata,
  created_at, source_key, content_hash, content_tsv
- SQL init files run in lexicographic order. Current sequence ends at 13
  (13_v08_tool_registry_seed.sql). Next available: 14, 15, 16.

READ BEFORE WRITING:
- infrastructure/sql/init/12_v08_tool_registry.sql — exact tool_registry schema
- infrastructure/sql/init/ listing — confirm all current file numbers

YOUR TASK — Three SQL files in infrastructure/sql/init/:

1. NEW FILE: infrastructure/sql/init/14_v09_workers_tables.sql

   Table clive_state.workers:
     worker_name      TEXT        PRIMARY KEY
                                  REFERENCES clive_state.tool_registry(tool_name),
     schedule_type    TEXT        NOT NULL
                                  CHECK (schedule_type IN ('cron','event_trigger')),
     cron_expression  TEXT,       -- populated when schedule_type = 'cron'
     trigger_event    TEXT,       -- populated when schedule_type = 'event_trigger'
     execution_scope  TEXT[]      NOT NULL,
     last_run_at      TIMESTAMPTZ,
     next_run_at      TIMESTAMPTZ

   Table clive_state.worker_runs:
     run_id           UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
     worker_name      TEXT        NOT NULL
                                  REFERENCES clive_state.workers(worker_name),
     triggered_at     TIMESTAMPTZ NOT NULL DEFAULT NOW(),
     completed_at     TIMESTAMPTZ,
     status           TEXT        NOT NULL DEFAULT 'running'
                                  CHECK (status IN
                                    ('running','success','error','skipped')),
     outcome_summary  TEXT,
     error_detail     TEXT

   Indexes:
     idx_workers_schedule_type      ON clive_state.workers (schedule_type)
     idx_worker_runs_worker_name    ON clive_state.worker_runs
                                       (worker_name, triggered_at)
     idx_worker_runs_status         ON clive_state.worker_runs
                                       (status, triggered_at)

   Grants:
     GRANT ALL ON clive_state.workers TO clive_app;
     GRANT ALL ON clive_state.worker_runs TO clive_app;

   Must be fully idempotent: IF NOT EXISTS on all CREATE statements.

2. NEW FILE: infrastructure/sql/init/15_v09_workers_seed.sql

   INSERT into clive_state.tool_registry (ON CONFLICT DO NOTHING) — two rows:

   a. tool_name = 'daily_digest'
      display_name = 'Daily Digest'
      version = '1.0.0'
      description = 'Summarises recent queries, actions, cost, feedback, and
                     system health; delivers via Telegram once daily'
      permission_scope = ARRAY['read:queries','read:actions','read:cost',
                               'read:feedback','write:telegram']
      health_status = 'healthy', enabled = TRUE, deprecated = FALSE

   b. tool_name = 'knowledge_maintenance'
      display_name = 'Knowledge Maintenance'
      version = '1.0.0'
      description = 'Identifies stale unaccessed chunks and flags for owner
                     review via confirmation gate; no autonomous deletion'
      permission_scope = ARRAY['read:storage','write:confirmations']
      health_status = 'healthy', enabled = TRUE, deprecated = FALSE

   INSERT into clive_state.workers (ON CONFLICT DO NOTHING) — two rows:

   a. worker_name = 'daily_digest'
      schedule_type = 'cron'
      cron_expression = '0 8 * * *'     -- 08:00 UTC daily
      execution_scope = ARRAY['read:queries','read:actions','read:cost',
                               'read:feedback','write:telegram']

   b. worker_name = 'knowledge_maintenance'
      schedule_type = 'cron'
      cron_expression = '0 9 * * 1'    -- 09:00 UTC every Monday
      execution_scope = ARRAY['read:storage','write:confirmations']

   NOTE: tool_registry rows must be inserted BEFORE workers rows in this
   file (FK dependency). Both use ON CONFLICT DO NOTHING.

3. NEW FILE: infrastructure/sql/init/16_v09_chunk_retrieval_tracking.sql

   Add columns to clive_search.chunks using ADD COLUMN IF NOT EXISTS:
     retrieval_count     INTEGER     NOT NULL DEFAULT 0
     last_retrieved_at   TIMESTAMPTZ          -- nullable; NULL = never retrieved

   Add partial index (fast scan for knowledge maintenance worker):
     CREATE INDEX IF NOT EXISTS idx_chunks_unaccessed
       ON clive_search.chunks (created_at)
       WHERE retrieval_count = 0;

   No grant changes needed (clive_search.chunks already grants to clive_app).
   Must be idempotent: ADD COLUMN IF NOT EXISTS, CREATE INDEX IF NOT EXISTS.

CONSTRAINTS:
- Schema only. Do not touch Block 8, Block 13, or Block 23 source code.
- clive_audit_writer has INSERT-only on the audit table — do not alter
  that privilege. New tables use clive_app only.
- No secrets in SQL files.

DONE WHEN:
- 14_v09_workers_tables.sql: idempotent, creates both tables with FK and indexes
- 15_v09_workers_seed.sql: four INSERTs with ON CONFLICT DO NOTHING, tool_registry
  rows before workers rows
- 16_v09_chunk_retrieval_tracking.sql: idempotent, adds two columns and index
- No filename conflicts with existing init files
- Changes committed to main
```

---

## Wave 1-B — Intelligence Agent (Block 12 policy document)

Open simultaneously with Wave 1-A. No schema dependency.

```
Read DECISIONS.md from the repo root and confirm the highest decision ID
before acting on anything else. If D-140 and D-141 are not present, stop
and tell me — do not proceed.

CONTEXT:
Block 12 (Context Window) is a partial block. The dynamic context allocation
in Block 8 works correctly but there is no standalone policy document
(roadmap, Block 12 partial status: "Dynamic allocation works inside Block 8;
no standalone policy document"). v0.9 closes Block 12 by producing that
document. Source code enforcement of Block 12 is Wave 2-B.

This wave is design and documentation only. No source code changes.

READ FIRST (all of these, before writing anything):
- src/query/query/context.py (full) — this is the canonical implementation
- src/query/query/handler.py (full) — call sequence for context assembly
- docs/spec/Block 8 - Query Service Source Code Specification.md

YOUR TASK:
Produce docs/spec/Block 12 - Context Window Policy.md

The document must have these six sections:

1. PURPOSE
   One paragraph: what Block 12 governs and why it is separate from Block 8.
   Block 8 assembles context. Block 12 is the policy that governs how.

2. TOTAL CONTEXT BUDGET
   State the exact MAX_CONTEXT_TOKENS value from context.py. Explain what
   it includes (all injected text except the user's live query).

3. PRIORITY TIERS
   Document the exact tiers as implemented in context.py. For each tier:
   - Tier name and number
   - Token allocation or limit (exact constant from context.py)
   - What is included
   - How overflows are handled (truncation rule)
   - Injection order in the final assembled prompt

   Based on current Block 8, the tiers are approximately:
     Tier 1 — System documents (personality + alignment rules): ALWAYS fits;
               hard limit; never truncated; system fails if they exceed budget
     Tier 2 — Conversation history: most-recent-last; drop oldest if over budget
     Tier 3 — Memory entities (Block 11): highest-similarity first; drop lowest
     Tier 4 — Retrieved chunks (Block 16 RAG): highest-similarity first; drop lowest
     Tier 5 — Available tools (Block 17 registry): name+description only; omit
               entirely if no budget remains (not partial omission)

   Adjust names, numbers, and constants to match exactly what context.py implements.
   Do not invent tiers that do not exist.

4. ALLOCATION ALGORITHM
   Step-by-step: how the budget is consumed, tier by tier. Which tiers are
   reserved first, what happens if earlier tiers consume all budget (what is
   left for later tiers), and what the fallback is for Tier 1 overflow (fatal
   error — should not occur in practice given typical system document sizes).

5. INVARIANTS
   Rules that always hold regardless of query:
   - System documents (Tier 1) are always present; never truncated
   - User query text is NOT counted against the context budget
   - No tier silently exceeds its allocation
   - Truncation is deterministic (not random)
   - LLM never sees tool permission_scope — name and description only

6. FUTURE NOTES
   What this policy does NOT yet govern:
   - Dynamic tier reweighting by query type
   - Per-surface budget differences (relevant at v0.11, two surfaces)
   - Worker context assembly (Block 10 workers do not use this policy)

ADDITIONAL SECTION (if applicable):
   ## Known Implementation Gaps
   If context.py has magic numbers not yet named, or behaviour that doesn't
   match a sensible policy: list each gap here. Do not fix it — flag it
   for Wave 2-B.

CONSTRAINTS:
- Do not change source code under any circumstances.
- The policy must describe what Block 8 actually does — not an ideal design.
- Do not make new decisions. Reference D-044 (dynamic allocation) and D-012
  (Block 12 context window) where relevant.

DONE WHEN:
- docs/spec/Block 12 - Context Window Policy.md exists
- All six sections present
- Policy accurately reflects current context.py implementation
- Any discrepancies documented in Known Implementation Gaps
- Changes committed to main
```

---

## Wave 2-A — Systems Agent (Block 13 scheduler + scope enforcement)

Wait for Wave 1-A to be committed before starting. Then open a new Claude Code session.

```
Read DECISIONS.md from the repo root and confirm the highest decision ID
before acting on anything else. If D-140 and D-141 are not present, stop.
Confirm infrastructure/sql/init/14_v09_workers_tables.sql exists.
If absent, stop — Wave 1-A must be committed first.

CONTEXT:
v0.9 adds Block 10 Workers. Workers are registered in Block 17 (tool_registry
+ workers tables). Block 13 owns the scheduler that runs workers on cron
schedules. Workers are asyncio coroutines co-located in the orchestrator
process — the same pattern as reminder_handler.reminder_poll().

Workers push to Block 23 via HTTP (same D-003-compliant pattern as all existing
push functions). Workers access the DB directly (same pattern as audit.py,
action.py, reminder_handler.py).

CURRENT STATE TO READ BEFORE CHANGING ANYTHING:
- src/orchestrator/orchestrator/main.py (full)
- src/orchestrator/orchestrator/reminder_handler.py (the pattern to follow)
- src/orchestrator/orchestrator/push.py (full — you will add one function)
- src/orchestrator/orchestrator/metrics.py (Prometheus counter patterns)
- src/orchestrator/pyproject.toml (dependencies)
- infrastructure/sql/init/14_v09_workers_tables.sql (exact schema)
- infrastructure/sql/init/15_v09_workers_seed.sql (worker names, scopes)

YOUR TASK — Four deliverables:

1. NEW FILE: src/orchestrator/orchestrator/workers/__init__.py
   Empty package init. Content: empty (or a single-line docstring
   "Block 10 — Worker modules for CLIVE v0.9.").

2. NEW FILE: src/orchestrator/orchestrator/scheduler.py

   Module-level docstring referencing D-140, D-141.

   CONSTANTS:
     SCHEDULER_TICK_SECONDS = 30       # poll interval
     DEFAULT_POOL_DSN (constructed from env vars, same pattern as reminder_handler)

   POOL:
     Module-level _pool: asyncpg.Pool | None = None
     async def init_pool() -> None  (follow reminder_handler.init_pool pattern)
     def _get_pool() -> asyncpg.Pool

   SCOPE ENFORCEMENT:
     SCOPE_TO_CAPABILITY = {
         'write:telegram':      'notify',
         'write:confirmations': 'request_confirmation',
     }

     def make_scoped_push(worker_name: str, execution_scope: list[str]) -> dict:
         """Return a dict of permitted push callables for this worker.

         Keys are capability names (str). Values are async callables.
         Workers access capabilities via scoped_push['notify'](message).
         If a key is absent, the capability is not permitted for this worker.
         Log WARN if a worker attempts to call an unlisted capability.
         """
         scoped: dict = {}
         if 'write:telegram' in execution_scope:
             scoped['notify'] = _push_worker_notification
         if 'write:confirmations' in execution_scope:
             scoped['request_confirmation'] = _push_worker_confirmation
         return scoped

   PUSH HELPERS (defined in scheduler.py, not push.py — workers are not
   CLIVEEvent-driven; these are imperative HTTP calls):

     async def _push_worker_notification(message_text: str) -> None:
         """Send a worker digest/notification to Block 23 /alert endpoint."""
         import uuid, httpx
         async with httpx.AsyncClient() as client:
             resp = await client.post(
                 f"{os.environ.get('TELEGRAM_SERVICE_URL','http://telegram:8082')}/alert",
                 json={
                     "event_id": str(uuid.uuid4()),
                     "severity": "info",
                     "title": "Worker notification",
                     "body": message_text,
                 },
                 timeout=10.0,
             )
             resp.raise_for_status()

     async def _push_worker_confirmation(
         action_type: str,
         action_target: str,
         action_description: str,
         chat_id: int,
         expires_at: str,     # ISO timestamp string
         action_request_id: str,
     ) -> None:
         """Insert pending_action row and push confirmation request to surface.
         
         Inserts into clive_state.pending_actions directly (no CLIVEEvent needed;
         worker is co-located in Block 13). Then calls push_confirmation_to_surface
         from push.py by constructing a minimal CLIVEEvent.
         D-006: this is the confirmation gate — no action taken without owner confirm.
         """
         (Implement: insert into pending_actions via _get_pool(), then publish
          ACTION_CONFIRMATION_REQUESTED event via bus. Follow action.py pattern.)

   WORKER DISPATCH TABLE:
     _WORKERS: dict[str, Any] = {}   # populated at startup via _load_dispatch()

     def _load_dispatch() -> None:
         """Lazy import of worker modules to allow compilation before they exist."""
         from .workers import daily_digest, knowledge_maintenance
         _WORKERS['daily_digest'] = daily_digest.run
         _WORKERS['knowledge_maintenance'] = knowledge_maintenance.run

   LOAD WORKERS FROM DB:
     async def _load_worker_configs() -> list[dict]:
         """Query workers table for all enabled cron workers.

         SELECT w.worker_name, w.cron_expression, w.execution_scope
         FROM clive_state.workers w
         JOIN clive_state.tool_registry t USING (tool_name)
         WHERE w.schedule_type = 'cron' AND t.enabled = TRUE
         """

   RUN A WORKER:
     async def _run_worker(worker_config: dict) -> None:
         """Execute one worker. Log run to worker_runs. Catch all exceptions.

         1. INSERT INTO clive_state.worker_runs
            (worker_name, status='running', triggered_at=NOW())
            — record run_id
         2. Build scoped_push from execution_scope.
         3. Call _WORKERS[worker_name](run_id, pool, scoped_push)
            — where pool = _get_pool()
         4. On return: UPDATE worker_runs SET status='success',
            completed_at=NOW(), outcome_summary=<return value>
         5. On exception: UPDATE worker_runs SET status='error',
            completed_at=NOW(), error_detail=str(e)
            — log.error("worker_failed", ...)
            — do NOT re-raise; scheduler must stay alive
         6. Increment Prometheus: worker_runs_total.labels(
                worker_name=..., status=...).inc()
         """

   SCHEDULER LOOP:
     async def scheduler_loop() -> None:
         """Long-running cron scheduler. Started from main.py via create_task().

         At startup:
           - Call _load_dispatch()
           - Load worker configs from DB
           - Compute initial next_run_at for each worker using croniter:
             croniter(cron_expression, datetime.now(UTC)).get_next(datetime)

         Loop (SCHEDULER_TICK_SECONDS sleep between iterations):
           - For each worker: if datetime.now(UTC) >= next_run_at:
               await _run_worker(config)
               Recompute next_run_at from current time
           - Handle asyncio.CancelledError: return cleanly
         """

   Add croniter to pyproject.toml dependencies.

3. MODIFY: src/orchestrator/orchestrator/metrics.py
   Add Prometheus counter (follow existing counter patterns in the file):
     worker_runs_total = Counter(
         "clive_worker_runs_total",
         "Total Block 10 worker runs by worker and outcome status",
         ["worker_name", "status"],
     )

4. MODIFY: src/orchestrator/orchestrator/main.py
   Import: from . import scheduler
   In main(), after existing pool inits:
     await scheduler.init_pool()
   In background tasks section:
     scheduler_task = asyncio.create_task(scheduler.scheduler_loop())
   In shutdown gather:
     include scheduler_task alongside timeout_task, reminder_task
   Update module docstring to note v0.9 worker scheduler.

CONSTRAINTS:
- D-003: workers push to Block 23 via HTTP (_push_worker_notification calls
  the Telegram service directly — Block 13 mediates, no block-to-block shortcut).
- D-006: _push_worker_confirmation inserts pending_action and emits
  action.confirmation_requested — the gate is enforced; no worker can delete
  without owner confirmation.
- D-025: worker_runs insert is idempotent on run_id (UUID PRIMARY KEY).
- croniter is the ONLY new dependency. Do not add others.
- Scheduler loop must tolerate individual worker failures and continue running.
- Use load_dotenv('/etc/clive/secrets.env') via existing main.py import.

DONE WHEN:
- src/orchestrator/orchestrator/workers/__init__.py exists
- src/orchestrator/orchestrator/scheduler.py exists with all components above
- metrics.py has worker_runs_total counter
- main.py imports and starts scheduler_task
- croniter in pyproject.toml
- Existing tests pass
- New unit tests (src/orchestrator/tests/test_v09_scheduler.py):
  - _load_worker_configs returns only enabled cron workers
  - _run_worker inserts worker_runs row with status='running' then 'success'
  - _run_worker sets status='error' on exception without re-raising
  - make_scoped_push returns 'notify' when write:telegram in scope
  - make_scoped_push does NOT return 'notify' when write:telegram absent
- Changes committed to main
```

---

## Wave 2-B — Intelligence Agent (Block 8 retrieval tracking + Block 12 enforcement)

Wait for Wave 1-A AND Wave 1-B to both be committed. Open simultaneously with Wave 2-A.

```
Read DECISIONS.md from the repo root and confirm the highest decision ID
before acting on anything else. If D-140 and D-141 are not present, stop.
Confirm BOTH of these exist before proceeding:
  - infrastructure/sql/init/16_v09_chunk_retrieval_tracking.sql
  - docs/spec/Block 12 - Context Window Policy.md
If either is missing, stop — both Wave 1 sessions must complete first.

CONTEXT:
Two changes to src/query/ for v0.9:

1. RETRIEVAL TRACKING: After each query, Block 8 updates retrieval_count
   and last_retrieved_at on the returned chunks. The knowledge maintenance
   worker (Wave 3-B) uses this to find stale chunks.

2. BLOCK 12 CLOSE: Make context.py's policy parameters explicit named
   constants and add a docstring cross-reference to the Block 12 policy doc.

READ FIRST (all of these):
- src/query/query/handler.py (full)
- src/query/query/context.py (full)
- src/query/query/db.py (DB access patterns)
- docs/spec/Block 12 - Context Window Policy.md (Wave 1-B output — required)
- infrastructure/sql/init/16_v09_chunk_retrieval_tracking.sql
  (column names: retrieval_count INTEGER, last_retrieved_at TIMESTAMPTZ)

CHANGE 1 — RETRIEVAL TRACKING:

After each successful query that returns chunks (chunk_ids list is non-empty),
update clive_search.chunks:

  UPDATE clive_search.chunks
  SET retrieval_count = retrieval_count + 1,
      last_retrieved_at = NOW()
  WHERE chunk_id = ANY($1::uuid[])

Rules:
- Run AFTER the query.response is emitted (do not block on it).
- Non-fatal: wrap in try/except Exception. On failure: log WARN with
  chunk_count and error. Do NOT re-raise. Query must succeed regardless.
- Use the existing DB pool in Block 8. Do not create a new pool.
- chunk_ids come from the retrieval result already assembled in handler.py.
  Find where chunk_ids are collected and add the update call after that point.
- If chunk_ids is empty (no RAG results): skip the update entirely.

CHANGE 2 — BLOCK 12 EXPLICIT ENFORCEMENT:

Read docs/spec/Block 12 - Context Window Policy.md (Wave 1-B).

In context.py:
a. Ensure every policy parameter is a named module-level constant.
   If any value appears as a magic number inline: extract it to a named
   constant. Match the name to the tier label in the Block 12 policy doc.
   Examples: MAX_CONTEXT_TOKENS, MAX_CONVERSATION_TURNS, MAX_CHUNKS_RETURNED,
   MAX_MEMORY_ENTITIES, MAX_TOOLS_TOKENS — whatever context.py currently uses.

b. Add to the top of context.py, after any existing module docstring or
   replacing it:
   """Context assembly for Block 8 queries.

   Follows Block 12 Context Window Policy:
   docs/spec/Block 12 - Context Window Policy.md

   Policy constants defined below correspond to the tiers in that document.
   All values here are the authoritative implementation parameters.
   Changes to these constants must be reflected in the policy document.
   """

c. If the Block 12 policy document (Wave 1-B) listed Known Implementation Gaps:
   address any gaps that are clarifications only (constant naming, comments).
   Do NOT change numerical values or assembly behaviour. If a gap requires
   changing the assembly logic, leave it and add a TODO comment referencing
   the gap.

CONSTRAINTS:
- D-043: retrieval tracking is a DB write within Block 8's own execution.
  Acceptable — same pattern as memory entity storage (v0.7).
- D-046: duplicate cache still applies. The retrieval count update runs
  AFTER the cache check — do not update counts on cache hits (only on
  fresh retrievals).
- D-044: do not change the context allocation logic. Constants and comments only.

DONE WHEN:
- Block 8 updates retrieval_count + last_retrieved_at after each fresh query
  that returns chunks (not on cache hits)
- Update is non-fatal (try/except, WARN log on failure)
- context.py has named constants for all policy parameters
- context.py module docstring references Block 12 policy document
- Existing tests pass
- New unit test: after a query that returns non-empty chunk_ids,
  retrieval_count increment is attempted (mock the DB call, verify it is
  called with the correct chunk_ids)
- Changes committed to main
```

---

## Wave 3-A — Intelligence Agent (Daily Digest Worker)

Wait for Wave 2-A AND Wave 2-B to both be committed. Then open a new session.

```
Read DECISIONS.md from the repo root and confirm the highest decision ID
before acting on anything else. If D-140 and D-141 are not present, stop.
Confirm src/orchestrator/orchestrator/scheduler.py exists.
If absent, stop — Wave 2-A must be committed first.

CONTEXT:
The scheduler (Wave 2-A) calls workers.daily_digest.run(run_id, pool, scoped_push)
once daily at 08:00 UTC. This session implements that function.

The daily digest is proactive. The owner does not request it.
It delivers a Telegram summary of the previous 24 hours.
It uses scoped_push['notify'](message) to send the message.
It must not query tables outside its declared scope:
  read:queries, read:actions, read:cost, read:feedback, write:telegram

READ FIRST:
- src/orchestrator/orchestrator/scheduler.py (Wave 2-A — understand run() signature)
- src/orchestrator/orchestrator/reminder_handler.py (DB pool pattern)
- infrastructure/sql/init/08_v04_tables.sql (conversation_turns schema)
- infrastructure/sql/init/07_v03_tables.sql (pending_actions schema)
- infrastructure/sql/init/09_v06_tables.sql (llm_usage schema)
- infrastructure/sql/init/15_v09_workers_seed.sql (declared scope)

FUNCTION SIGNATURE (must match scheduler.py exactly):
  async def run(run_id: str, pool: asyncpg.Pool, scoped_push: dict) -> str:
    """Daily digest worker. Returns outcome_summary string."""

DATA QUERIES (all use pool parameter, 24h window = NOW() - INTERVAL '24 hours'):

  a. Query count (read:queries):
     SELECT COUNT(*) FROM clive_state.conversation_turns
     WHERE role = 'user' AND created_at >= NOW() - INTERVAL '24 hours'
     → integer

  b. Confirmed actions (read:actions):
     SELECT action_type, COUNT(*) as n
     FROM clive_state.pending_actions
     WHERE status = 'confirmed'
       AND resolved_at >= NOW() - INTERVAL '24 hours'
     GROUP BY action_type
     → list of (action_type, count) — may be empty

  c. LLM spend (read:cost):
     SELECT COALESCE(SUM(cost_usd), 0)
     FROM clive_state.llm_usage
     WHERE created_at >= NOW() - INTERVAL '24 hours'
     → Decimal / float

  d. Feedback count (read:feedback):
     SELECT COUNT(*) FROM clive_state.feedback
     WHERE submitted_at >= NOW() - INTERVAL '24 hours'
     → integer

  e. System health: always "healthy" (if the worker is running, system is up)

DIGEST FORMAT:
  Line 1: "📊 Daily Digest — {DD Mon YYYY}"  (e.g. "16 May 2026"; no leading zero)
  Blank line
  "Queries: {N}"
  "Actions: {summary}"   where summary is:
    - "none" if no confirmed actions
    - "{total} ({breakdown})" e.g. "3 (2 web_search, 1 reminder.schedule)"
  "Cost: ${amount:.4f}"  (4 decimal places, e.g. "$0.0431")
  If feedback_count > 0: "Feedback: {N} poor-quality response(s) tagged"
     (omit this line entirely if feedback_count = 0)
  "System: healthy"

  Max message length: 800 characters. If over limit, truncate actions breakdown
  to totals only.

DELIVERY:
  notify = scoped_push.get('notify')
  if notify:
      await notify(digest_text)
  else:
      log.warning("daily_digest_no_notify_scope")

RETURN VALUE:
  Return a short outcome_summary string for worker_runs.outcome_summary.
  Example: "Sent: 8 queries, 2 actions, $0.0431"
  If delivery failed (exception): include "delivery_failed" in summary.

ERROR HANDLING:
  Each data query is individually wrapped in try/except.
  On failure: use a placeholder string for that field (e.g. "unknown").
  Log WARN with the field name and error.
  Do NOT abort — always attempt to send a partial digest.

YOUR TASK:
CREATE src/orchestrator/orchestrator/workers/daily_digest.py

Also ensure scheduler.py has the real import (not a stub):
  from .workers import daily_digest
  and 'daily_digest': daily_digest.run in the dispatch table.
If scheduler.py already has the real import from your read, no change needed.

CONSTRAINTS:
- D-003: only use scoped_push['notify'] for Telegram delivery.
  Do not import or call Block 23 (Telegram service) directly.
- Do not import push.py in daily_digest.py.
- Do not query tables not in the declared scope
  (no chunks, no tool_registry, no system_documents).
- Secrets: use pool parameter (passed in by scheduler). No new DSN construction.

DONE WHEN:
- src/orchestrator/orchestrator/workers/daily_digest.py exists
- run() signature matches scheduler expectation
- All five data sources queried
- Digest format matches specification
- Individual query failures are non-fatal
- Existing tests pass
- New tests (src/orchestrator/tests/test_v09_daily_digest.py):
  - run() returns non-empty outcome_summary string
  - run() calls scoped_push['notify'] with message containing "Daily Digest"
  - run() includes "unknown" for a field when DB query raises an exception
  - run() omits feedback line when feedback_count = 0
- Changes committed to main
```

---

## Wave 3-B — Intelligence Agent (Knowledge Maintenance Worker)

Open simultaneously with Wave 3-A.

```
Read DECISIONS.md from the repo root and confirm the highest decision ID
before acting on anything else. If D-140 and D-141 are not present, stop.
Confirm src/orchestrator/orchestrator/scheduler.py exists.
If absent, stop — Wave 2-A must be committed first.

CONTEXT:
The scheduler calls workers.knowledge_maintenance.run(run_id, pool, scoped_push)
weekly. This session implements that function and its confirmation handler.

CRITICAL CONSTRAINT: This worker NEVER deletes chunks autonomously.
D-006 applies. The worker only PRESENTS a confirmation request to the owner.
Deletion occurs only after the owner explicitly confirms via /confirm_action.

This worker uses scoped_push['request_confirmation'](...) to present the
confirmation gate. Its declared scope: read:storage, write:confirmations.

READ FIRST:
- src/orchestrator/orchestrator/scheduler.py (Wave 2-A — run() signature,
  _push_worker_confirmation signature)
- src/orchestrator/orchestrator/main.py (dispatch_action_confirmed — you will
  add a branch here)
- src/orchestrator/orchestrator/action.py (pending_actions insert pattern)
- infrastructure/sql/init/16_v09_chunk_retrieval_tracking.sql
  (confirms retrieval_count column on clive_search.chunks)
- infrastructure/sql/init/07_v03_tables.sql (pending_actions schema)
- infrastructure/sql/init/15_v09_workers_seed.sql (declared scope)

FUNCTION SIGNATURES (must match scheduler.py):
  async def run(run_id: str, pool: asyncpg.Pool, scoped_push: dict) -> str:
  async def handle_prune_confirmed(event: CLIVEEvent) -> None:

ENVIRONMENT VARIABLE:
  KNOWLEDGE_MAINTENANCE_THRESHOLD_DAYS (int, default=90)
  Read with: int(os.environ.get('KNOWLEDGE_MAINTENANCE_THRESHOLD_DAYS', 90))

WHAT run() DOES:

1. Query stale chunks:
   SELECT chunk_id, source_attribution, created_at
   FROM clive_search.chunks
   WHERE retrieval_count = 0
     AND created_at < NOW() - INTERVAL '<threshold_days> days'
   ORDER BY created_at ASC
   LIMIT 5

   If result is empty:
     log.info("knowledge_maintenance_no_stale_chunks")
     return "No stale chunks found (threshold: {threshold_days} days)"

2. Build action_description:
   "Knowledge maintenance: {N} chunk(s) not retrieved in {threshold_days}+ days:\n"
   + one line per chunk: "  - '{source_attribution}' (ingested {days_ago} days ago)"
   + "\nConfirm to delete all listed chunks. Cancel to keep them."

3. Build action_target JSON string (stored in pending_actions.action_target):
   JSON array:
   [{"chunk_id": "<uuid>", "source": "<source_attribution>", "days_old": <N>}, ...]

4. Call scoped_push.get('request_confirmation'):
   import uuid, datetime
   action_request_id = str(uuid.uuid4())
   chat_id = int(os.environ.get('TELEGRAM_OWNER_CHAT_ID', 0))
   expires_at = (datetime.datetime.now(datetime.timezone.utc)
                 + datetime.timedelta(days=7)).isoformat()

   request_confirmation = scoped_push.get('request_confirmation')
   if request_confirmation:
       await request_confirmation(
           action_type='knowledge.prune',
           action_target=json.dumps(chunk_list),  # list of dicts above
           action_description=action_description,
           chat_id=chat_id,
           expires_at=expires_at,
           action_request_id=action_request_id,
       )
   else:
       log.warning("knowledge_maintenance_no_confirmation_scope")

5. Return: "Flagged {N} chunk(s) for review (threshold: {threshold_days} days)"

WHAT handle_prune_confirmed() DOES:
Called by main.py when action.confirmed arrives with action_type='knowledge.prune'.

1. Parse chunk_ids from event.payload:
   action_target = event.payload.get('action_target', '[]')
   chunks = json.loads(action_target)   # list of dicts with 'chunk_id'
   chunk_ids = [c['chunk_id'] for c in chunks]

2. DELETE FROM clive_search.chunks WHERE chunk_id = ANY($1::uuid[])
   Use the module-level pool (init via init_pool called from main.py).

3. Log: log.info("knowledge_prune_complete", chunk_count=len(chunk_ids))

4. Push outcome notification via a direct HTTP call to Block 23 /alert:
   (handle_prune_confirmed does not have scoped_push — use _push_prune_complete
   helper that calls telegram /alert directly like push.py does)
   Message: "Knowledge maintenance: {N} stale chunk(s) deleted."
   Severity: "info"

   Or alternatively: you may import and reuse push_alert_to_surface from push.py
   by constructing a minimal CLIVEEvent. Choose the simpler approach and
   document the choice.

POOL SETUP:
  async def init_pool() -> None (follow reminder_handler.init_pool pattern)
  Module-level _pool: asyncpg.Pool | None = None
  def _get_pool() -> asyncpg.Pool

MODIFY src/orchestrator/orchestrator/main.py:
  a. Import: from .workers import knowledge_maintenance
  b. In dispatch_action_confirmed, add after the reminder.schedule branch:
       elif action_type == 'knowledge.prune':
           await knowledge_maintenance.handle_prune_confirmed(event)
  c. In main(), after existing inits:
       await knowledge_maintenance.init_pool()
  d. Update module docstring to note v0.9 knowledge maintenance worker.

MODIFY src/orchestrator/orchestrator/scheduler.py:
  Replace stub import for 'knowledge_maintenance' with:
    from .workers import knowledge_maintenance
  Update dispatch table: 'knowledge_maintenance': knowledge_maintenance.run
  (If scheduler.py was already written without stubs, just add the real import.)

CONSTRAINTS:
- D-006: run() must use the confirmation gate (request_confirmation). 
  If scoped_push does not have 'request_confirmation', log WARN and return
  without presenting any confirmation. Do NOT fall back to direct deletion.
- D-003: handle_prune_confirmed pushes to Block 23 via HTTP. No direct import
  of Telegram library or bot API. Follow push.py pattern.
- max 5 chunks per run (LIMIT 5 in query). Never present more.
- Only read clive_search.chunks (read:storage scope). Do not query other tables.
- Secrets via load_dotenv('/etc/clive/secrets.env') already in main.py.

DONE WHEN:
- src/orchestrator/orchestrator/workers/knowledge_maintenance.py exists
- run() queries stale chunks (retrieval_count = 0, age > threshold)
- run() returns "No stale chunks found" when nothing is stale
- run() calls request_confirmation when stale chunks found
- handle_prune_confirmed() deletes chunks and notifies owner
- main.py handles 'knowledge.prune' in dispatch_action_confirmed
- main.py calls knowledge_maintenance.init_pool()
- scheduler.py has real import and dispatch entry
- Existing tests pass
- New tests (src/orchestrator/tests/test_v09_knowledge_maintenance.py):
  - run() returns "No stale chunks found" when query is empty
  - run() calls request_confirmation when 3 stale chunks found
  - run() limits batch to 5 even when 10 stale chunks exist
  - handle_prune_confirmed() executes DELETE with correct chunk_ids
- Changes committed to main
```

---

## Wave 4 — Infrastructure Agent (Grafana panel + CI verification)

Wait for ALL prior waves to be committed. This is the final session.

```
Read DECISIONS.md from the repo root and confirm the highest decision ID
before acting on anything else.

CONTEXT:
CLIVE v0.9 implementation is complete. Your task: add a Grafana worker
run panel to the existing dashboard, then verify CI will pass.

PART 1 — GRAFANA WORKER RUN PANEL

The orchestrator emits Prometheus counter:
  clive_worker_runs_total{worker_name, status}
(Added in Wave 2-A to src/orchestrator/orchestrator/metrics.py.)

READ FIRST:
- infrastructure/compose/ — locate all Grafana dashboard provisioning JSON files
- Find the dashboard that contains the event bus observability panels (D-134)
- Read the existing panel JSON to understand format, data source name, and uid

ADD a panel to that existing dashboard:
  Title: "Worker Runs (24h)"
  Panel type: stat panel showing three values, or time-series
  Data source: same as existing panels in the dashboard

  Queries (Prometheus, 24h window):
    A: increase(clive_worker_runs_total{status="success"}[24h])
       Legend: "Success"
    B: increase(clive_worker_runs_total{status="error"}[24h])
       Legend: "Error"
    C: increase(clive_worker_runs_total{status="skipped"}[24h])
       Legend: "Skipped"

  If the dashboard supports it: add worker_name as a label selector.
  If the stat panel supports multiple series: show one stat per status.

  Place the panel at the end of the existing row (or a new row if the
  existing row is full). Do not create a new dashboard file.

PART 2 — CI VERIFICATION

1. SQL IDEMPOTENCY
   Read all three new SQL files:
   - infrastructure/sql/init/14_v09_workers_tables.sql
   - infrastructure/sql/init/15_v09_workers_seed.sql
   - infrastructure/sql/init/16_v09_chunk_retrieval_tracking.sql
   Confirm: every CREATE uses IF NOT EXISTS; every INSERT uses ON CONFLICT DO NOTHING;
   every ALTER uses IF NOT EXISTS.
   If any statement is not idempotent: flag the exact file and line number.
   Do not fix — report to the owner with specific file and line reference.

2. CI PIPELINE COVERAGE
   Read .github/workflows/ci.yml.
   Confirm:
   a. New SQL files (14, 15, 16) are picked up by the idempotency test.
      If the test uses a glob covering infrastructure/sql/init/, confirm it
      matches these filenames. If an explicit list exists, add the three files.
   b. Unit test runner covers src/orchestrator/tests/ for the new scheduler
      and worker tests.
   c. src/query/tests/ is covered for the new retrieval tracking test.
   d. croniter is listed in src/orchestrator/pyproject.toml and will be
      installed in the CI test environment. If CI uses a separate requirements
      file, add croniter there too.
   Make any necessary ci.yml changes directly.

3. TEST COVERAGE VERIFICATION
   Confirm the following tests exist. For each, read the test file and verify
   the assertion is present. If absent: add it to docs/v0.9-ci-gap-report.md.

   src/orchestrator/tests/test_v09_scheduler.py:
   - _load_worker_configs returns only enabled cron workers
   - _run_worker sets status='success' on worker success
   - _run_worker sets status='error' on exception without re-raising
   - make_scoped_push includes 'notify' when write:telegram in scope
   - make_scoped_push excludes 'notify' when write:telegram absent

   src/orchestrator/tests/test_v09_daily_digest.py:
   - run() returns non-empty outcome_summary
   - run() calls scoped_push['notify'] with message containing "Daily Digest"
   - run() handles DB failure on one source without aborting

   src/orchestrator/tests/test_v09_knowledge_maintenance.py:
   - run() returns "No stale chunks found" when no stale chunks
   - run() calls request_confirmation when stale chunks found
   - run() limits batch to 5 even when more exist
   - handle_prune_confirmed() executes DELETE with correct chunk_ids

   src/query/tests/:
   - retrieval_count update attempted after query returning non-empty chunk_ids

4. DOCKER COMPOSE INIT ORDER
   Confirm lexicographic order runs files 14, 15, 16 after 13.
   Note the existing double-10 filenames (10_v07_actions_tables.sql and
   10_v07_memory_tables.sql) — confirm these do not interfere with 14-16.
   No change needed if order is correct — confirm only.

DONE WHEN:
- Grafana panel added to existing dashboard JSON
- SQL idempotency confirmed (or gap report with exact file + line references)
- croniter dependency confirmed in CI environment
- All test assertions confirmed present, or gap report at docs/v0.9-ci-gap-report.md
- CI pipeline changes committed if any were needed
- Changes committed to main
```

---

## Sign-off (Owner — after Wave 4 completes)

Once the Infrastructure Agent session reports clean (or gap report is addressed),
v0.9 is ready for sign-off. The owner records:

- **D-142** — CLIVE v0.9 signed off (with date and confirmation all nine
  acceptance criteria from D-141 are met)

This follows the established pattern: D-094 (v0.1), D-104 (v0.2), D-110 (v0.3),
D-120 (v0.4), D-124 (v0.5), D-127 (v0.6), D-130/D-133 (v0.7), D-139 (v0.8).

---

*v0.9 prompt document — produced by Architect 2026-05-16*  
*Do not edit without a corresponding decision record.*
